# Local backend development (`go run . serve` from backend/)
# Copy to backend/.env

APP_PORT=8080
APP_ENV=local
APP_CLIENT=web

# Public origin when testing MCP or API URLs outside localhost.
# APP_PUBLIC_URL=http://localhost:8080
# APP_MCP_PUBLIC_URL=http://localhost:8080/api/mcp

import { Box, Input, Select } from '@mui/material';

export default function FilterItem() {
  return (
    <Box>
      <Select size='small' />
      <Select size='small' />
      <Input size='small' />
    </Box>
  );
}

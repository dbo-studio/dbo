pre-push:
  commands:
    front-check:
      run: npm run biome check frontend/src --no-errors-on-unmatched --files-ignore-unknown=true --colors=off
    front-test:
      root: "frontend"
      run: npm run test
    backend-check:
      root: "backend"
      run: golangci-lint run
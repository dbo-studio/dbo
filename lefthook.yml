pre-push:
  commands:
    # front-test:
    #   run: npm --prefix frontend run test
    front-format:
      run: npm --prefix frontend run format
    backend-format:
      root: "backend"
      run: go fmt $(go list ./... | grep -v /vendor/)
pre-push:
  commands:
    front-check:
      run: npm run biome check frontend/src --colors=off
    front-test:
      root: "frontend"
      run: npm run test
    backend-check:
      root: "backend"
      run: golangci-lint run